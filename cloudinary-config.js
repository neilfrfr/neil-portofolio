// ============================================================
// PASTE YOUR CLOUDINARY CLOUD NAME AND UPLOAD PRESET BELOW.
// Get these from: cloudinary.com Console > Dashboard (cloud name)
// and Settings > Upload > Upload presets (create an UNSIGNED preset).
// See README.md for the full step-by-step.
//
// These values are safe to be public — an unsigned preset can be
// restricted (folder, file types, max size) from the Cloudinary
// console so it can't be abused even though it's exposed client-side.
// ============================================================
export const cloudinaryConfig = {
  cloudName: "eegihvm1",
  uploadPreset: "portfolio_uploads"
};
